name: 3D Commits
on:
  schedule:
    - cron: "0 0 * * *"
  
  workflow_dispatch:

  push:
    branches:
      - master
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    name: Generate 3D Commits
    permissions:
      contents: write 
      
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          
          filename: profile-3d-commits.svg
          
          base: ""
          plugin_isocalendar: yes
          plugin_isocalendar_duration: full-year 
